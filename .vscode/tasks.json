{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "npm: compile",
      "type": "shell",
      "command": "npm",
      "args": ["run", "compile"],
      "linux": { "command": "zsh", "args": ["-ic", "npm run compile"] },
      "options": { "cwd": "${workspaceFolder}" },
      "group": "build",
      "problemMatcher": "$tsc"
    }
  ]
}
